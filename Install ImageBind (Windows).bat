@echo off
rem Convenience shortcut: installs the optional ImageBind (Huge) model into the
rem local .venv. Run this after the first normal start, then restart the app.
call "%~dp0bootstrap\windows\install_optional_imagebind.bat"
