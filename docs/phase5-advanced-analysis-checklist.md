# Phase 5 Advanced Analysis Checklist

- [ ] Open an existing projection from `output/projections`.
- [ ] Generate a cluster report from a clustered projection.
- [ ] Confirm a non-clustered projection shows the no-cluster-data message.
- [ ] Isolate a cluster from the cluster report and confirm the canvas count changes.
- [ ] Compare two different model projections.
- [ ] Compare UMAP and t-SNE projections for the same image set.
- [ ] Confirm comparison warns that raw axes are not directly comparable.
- [ ] Save a session with display settings and filters.
- [ ] Reload the saved session and confirm projection/filter/display state is restored.
- [ ] Export a standalone HTML package.
- [ ] Open the exported `index.html` locally and confirm the plot renders.
- [ ] Combine cluster, filename, subfolder and score filters.
- [ ] Clear all filters and confirm the full projection returns.
- [ ] Verify search result highlighting still works with filters active.
- [ ] Verify the original projection TSV download remains unchanged.
